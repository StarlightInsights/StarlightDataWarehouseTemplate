name: 'starlight'
version: '1.0.0'

profile: 'starlight'

model-paths: ["models"]
seed-paths: ["seeds"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  starlight:
    example:
      +materialized: view
