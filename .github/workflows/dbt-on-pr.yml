name: dbt on PR

on:
  push:
    branches:
      - '*'

jobs:
  execute-dbt-commands:
    uses: ./.github/workflows/dbt.yml
    with:
      commands: |
        cd starlight
        dbt debug --target github
        dbt deps --target github
        echo 1
