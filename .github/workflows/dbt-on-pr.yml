name: Call Reusable Workflow

on:
  push:
    branches:
      - '*'

jobs:
  use-reusable:
    uses: ./.github/workflows/dbt.yml
    with:
      commands: |
        cd starlight
        dbt debug
        dbt deps
