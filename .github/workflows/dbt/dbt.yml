name: Reusable Workflow

on:
  workflow_call:
    inputs:
      script:
        required: true
        type: string

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Execute provided script
        run: |
          ${{ inputs.script }}
